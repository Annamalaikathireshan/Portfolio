---
const { strip } = Astro.props;
---

<div class="architecture-strip reveal">
  {strip.layers.map((layer: string, i: number) => (
    <>
      {i > 0 && <span class="arch-arrow" aria-hidden="true">→</span>}
      <span class="arch-layer">{layer}</span>
    </>
  ))}
</div>
